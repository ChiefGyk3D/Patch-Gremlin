# Configuration files with secrets
config.sh
!config.example.sh

# Doppler files
.doppler.yaml

# System files
.DS_Store
*~
*.swp
*.swo

# Logs
*.log
# ...but the test fixtures ARE the real log formats we parse, so they must be
# tracked. Without this the suite passes locally and fails in CI.
!tests/fixtures/*.log

# Test artifacts
tests/**/*.tmp
.bats-*
